spring:
  application:
    name: piedrazul
  datasource:
    url: jdbc:postgresql://localhost:5432/piedrazul
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: "${JWT_SECRET:bCrzAJipG9zqoa1R1yOTZJ0Slm+i5I+ffOZ6ogq33so=}"
  access-expiration: 900000
  refresh-expiration: 604800000